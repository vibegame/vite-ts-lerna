import * as radash from "radash";

export const sum = (a: number, b: number) => a + b;
export const subtract = (a: number, b: number) => a - b;
export const group = radash.group;
export const capitalize = radash.capitalize;
export const uppercase = radash.upperize;
